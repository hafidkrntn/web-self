---
---

<section id="home" class="hero">
  <div class="hero-inner">
    <h1 class="hero-name fade-in stagger-1">Muhamad Hafid Kurnianton</h1>
    <p class="hero-role fade-in stagger-2">Software Engineer</p>
    <p class="hero-bio fade-in stagger-3">
      I build software that solves real problems,
      from enterprise systems to modern web products,
      with a focus on simplicity, performance,
      and long-term maintainability.
    </p>
  </div>
</section>

<style>
  .hero {
    min-height: 100dvh;
    display: flex;
    align-items: center;
    max-width: var(--content-wide);
    margin: 0 auto;
    padding: var(--space-10) var(--space-5);
  }

  .hero-inner {
    max-width: var(--content-narrow);
  }

  .hero-name {
    margin-bottom: var(--space-6);
  }

  .hero-role {
    font-size: 1.25rem;
    font-weight: 500;
    color: var(--text-secondary);
    margin-bottom: var(--space-7);
  }

  .hero-bio {
    font-size: 1.125rem;
    max-width: 500px;
    line-height: 1.8;
  }
</style>
